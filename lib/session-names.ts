/** Session names (exact labels); matched case-insensitively against `sessions.name`. */
export const TRACKED_SESSION_NAMES = [
  "Friday Afternoon",
  "Saturday Morning",
  "Sunday Morning",
] as const;
